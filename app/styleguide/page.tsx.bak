/**
 * /styleguide â€” the design system, rendered with real content.
 *
 * Deliberately not linked from anywhere. It exists so type, colour and
 * component decisions can be judged in one place before they get spread
 * across the site. Delete it before launch, or keep it as evidence of
 * process â€” recruiters who find it tend to like that it exists.
 */

const SWATCHES = [
  { name: "paper", hex: "#F1F3F0", note: "Page background" },
  { name: "paper-raised", hex: "#FAFBF9", note: "Cards, device chrome" },
  { name: "ink", hex: "#141A17", note: "Body and headlines" },
  { name: "ink-muted", hex: "#6B7A72", note: "Secondary text, eyebrows" },
  { name: "line", hex: "#D5DAD4", note: "Hairline rules" },
  { name: "accent", hex: "#3B2FBF", note: "Links, focus, one CTA" },
  { name: "accent-soft", hex: "#EBE9FB", note: "Accent backgrounds" },
];

const STATUSES = [
  { key: "available", label: "Available for work", color: "var(--color-status-available)" },
  { key: "open", label: "Open to offers", color: "var(--color-status-open)" },
  { key: "closed", label: "Not available", color: "var(--color-status-closed)" },
];

const PLATFORMS = ["iOS", "Flutter", "Android"];

function Section({
  label,
  children,
}: {
  label: string;
  children: React.ReactNode;
}) {
  return (
    <section className="rule pt-8 mt-16">
      <p className="eyebrow mb-8">{label}</p>
      {children}
    </section>
  );
}

export default function Styleguide() {
  return (
    <main className="mx-auto max-w-5xl px-6 py-20">
      <p className="eyebrow">Design system</p>
      <h1
        className="mt-4 font-[family-name:var(--font-display)] font-bold"
        style={{
          fontSize: "var(--text-display)",
          lineHeight: "var(--text-display--line-height)",
          letterSpacing: "var(--text-display--letter-spacing)",
        }}
      >
        Precise
        <br />
        and light
      </h1>
      <p
        className="mt-6 max-w-xl text-[color:var(--color-ink-muted)]"
        style={{
          fontSize: "var(--text-lead)",
          lineHeight: "var(--text-lead--line-height)",
        }}
      >
        Every value below is a token. Nothing in the site hardcodes a colour or
        a size â€” change it here and it changes everywhere.
      </p>

      {/* ---------------------------------------------------------- */}
      <Section label="Colour">
        <div className="grid grid-cols-2 gap-x-8 gap-y-5 sm:grid-cols-3">
          {SWATCHES.map((s) => (
            <div key={s.name}>
              <div
                className="h-20 w-full rounded-sm"
                style={{
                  backgroundColor: s.hex,
                  border: "1px solid var(--color-line)",
                }}
              />
              <p className="mt-2 font-[family-name:var(--font-mono)] text-xs">
                {s.name}
              </p>
              <p className="font-[family-name:var(--font-mono)] text-xs text-[color:var(--color-ink-muted)]">
                {s.hex}
              </p>
              <p className="mt-1 text-xs text-[color:var(--color-ink-muted)]">
                {s.note}
              </p>
            </div>
          ))}
        </div>
      </Section>

      {/* ---------------------------------------------------------- */}
      <Section label="Type scale">
        <div className="space-y-8">
          <div>
            <p className="eyebrow mb-2">Display Â· Bricolage Grotesque 700</p>
            <p
              className="font-[family-name:var(--font-display)] font-bold"
              style={{
                fontSize: "var(--text-display)",
                lineHeight: "var(--text-display--line-height)",
                letterSpacing: "var(--text-display--letter-spacing)",
              }}
            >
              Builds and teaches
            </p>
          </div>

          <div>
            <p className="eyebrow mb-2">Title Â· Bricolage Grotesque 600</p>
            <p
              className="font-[family-name:var(--font-display)] font-semibold"
              style={{
                fontSize: "var(--text-title)",
                lineHeight: "var(--text-title--line-height)",
                letterSpacing: "var(--text-title--letter-spacing)",
              }}
            >
              Maktoub â€” Mobile Marketplace
            </p>
          </div>

          <div>
            <p className="eyebrow mb-2">Lead Â· Public Sans</p>
            <p
              className="max-w-2xl"
              style={{
                fontSize: "var(--text-lead)",
                lineHeight: "var(--text-lead--line-height)",
              }}
            >
              An iOS marketplace application built with SwiftUI, featuring
              API-driven listings, multimedia content and a responsive interface.
            </p>
          </div>

          <div>
            <p className="eyebrow mb-2">Body Â· Public Sans</p>
            <p className="max-w-2xl leading-relaxed">
              Mobile developer with three years building native iOS applications
              with Swift and SwiftUI, cross-platform apps with Flutter, and
              native Android. I also teach mobile development at TEK-UP, where I
              supervise student projects and run technical reviews.
            </p>
          </div>

          <div>
            <p className="eyebrow mb-2">Mono Â· JetBrains Mono</p>
            <p className="font-[family-name:var(--font-mono)] text-sm">
              SwiftUI Â· level 5 Â· 3 years
            </p>
          </div>
        </div>
      </Section>

      {/* ---------------------------------------------------------- */}
      <Section label="Platform tags">
        <p className="mb-5 max-w-xl text-sm text-[color:var(--color-ink-muted)]">
          The structural device for the whole site. Used instead of decorative
          numbering, because the platform is information the reader actually
          needs â€” a numbered list would only be decoration.
        </p>
        <div className="flex flex-wrap gap-2">
          {PLATFORMS.map((p) => (
            <span
              key={p}
              className="eyebrow border px-2.5 py-1"
              style={{ borderColor: "var(--color-line)" }}
            >
              {p}
            </span>
          ))}
        </div>
      </Section>

      {/* ---------------------------------------------------------- */}
      <Section label="Availability">
        <p className="mb-5 max-w-xl text-sm text-[color:var(--color-ink-muted)]">
          Driven live from Firestore. Colour is semantic here, never decorative
          â€” and the label carries the meaning on its own, so the dot is
          redundant rather than load-bearing.
        </p>
        <div className="flex flex-col gap-3">
          {STATUSES.map((s) => (
            <span key={s.key} className="flex items-center gap-2.5 text-sm">
              <span
                aria-hidden
                className="inline-block h-2 w-2 rounded-full"
                style={{ backgroundColor: s.color }}
              />
              <span style={{ color: s.color }}>{s.label}</span>
            </span>
          ))}
        </div>
      </Section>

      {/* ---------------------------------------------------------- */}
      <Section label="Actions">
        <div className="flex flex-wrap items-center gap-4">
          <button
            className="px-5 py-3 text-sm font-medium transition-opacity hover:opacity-90"
            style={{
              backgroundColor: "var(--color-accent)",
              color: "var(--color-paper)",
            }}
          >
            Contact me
          </button>

          <button
            className="border px-5 py-3 text-sm font-medium transition-colors"
            style={{ borderColor: "var(--color-ink)" }}
          >
            Download CV
          </button>

          <a href="#" className="link-underline text-sm">
            Read the case study
          </a>
        </div>
        <p className="mt-5 max-w-xl text-sm text-[color:var(--color-ink-muted)]">
          One filled button per screen. If everything is emphasised, nothing is
          â€” the recruiter should never have to decide which action matters.
        </p>
      </Section>

      {/* ---------------------------------------------------------- */}
      <Section label="Skill row">
        <div className="max-w-md">
          {[
            { name: "SwiftUI", level: 5, years: 3 },
            { name: "Flutter", level: 5, years: 3 },
            { name: "Kotlin", level: 4, years: 2 },
            { name: "AWS", level: 3, years: 2 },
          ].map((s) => (
            <div
              key={s.name}
              className="rule flex items-baseline justify-between py-3"
            >
              <span className="text-sm">{s.name}</span>
              <span className="font-[family-name:var(--font-mono)] text-xs text-[color:var(--color-ink-muted)]">
                {s.level}/5 Â· {s.years}y
              </span>
            </div>
          ))}
        </div>
        <p className="mt-5 max-w-xl text-sm text-[color:var(--color-ink-muted)]">
          Numbers, not progress bars. A bar claiming &ldquo;Swift 87%&rdquo;
          means nothing and recruiters have learned to skip them.
        </p>
      </Section>
    </main>
  );
}
